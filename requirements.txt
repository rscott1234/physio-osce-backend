# Web framework
flask==3.0.3
flask-cors==4.0.0

# OpenAI API client (new SDK, works with `OpenAI(api_key=...)`)
openai==1.42.0

# (Optional) for better JSON parsing and HTTP reliability
httpx==0.27.2

